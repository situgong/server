I will fix the `cargo run` ambiguity by specifying the default binary in `Cargo.toml`.

1. **Modify** **`Cargo.toml`**: Add `default-run = "linguaspark-server"` to the `[package]` section. This ensures `cargo run` executes the main server instead of the `inspect` utility.
2. **Verify Server**: Run `cargo run` to start the server and confirm that the new WASM-based translation engine initializes correctly without errors.

